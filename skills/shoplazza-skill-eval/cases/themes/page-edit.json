[
  {
    "case_id": "themes.page-edit-01",
    "type": "behavior",
    "skill": "shoplazza-themes",
    "operation": "themes.update",
    "scenario": "改轮播图某一帧的标题 — first step opens the session on the named theme; the plan copies the block target and uses update_slot",
    "input": "主题 3f2a9c1e-7b4d-4e8a-9c21-5d6e7f809a1b 的首页，把轮播图第一张幻灯片的标题改成 Summer Sale",
    "expected": {
      "route_to_skill": "shoplazza-themes",
      "access_tier": "shortcut",
      "command_prefix": "themes +page",
      "flags_must_contain": [
        "3f2a9c1e-7b4d-4e8a-9c21-5d6e7f809a1b",
        "--template index"
      ],
      "flags_must_not_contain": [
        "--promote",
        "--publish",
        "--theme "
      ],
      "must_ask": false,
      "semantic_rubric": [
        "the first read is themes +page on that theme's index template (which returns the oseid), followed by a single-card read with --session <oseid> --section <sid> --include schema",
        "the planned write is themes +edit with update_slot on the block-row target copied from the read (<sid>.blocks[0] form), writing only the heading field with Summer Sale verbatim",
        "the same --session and -t are used on every call; the plan stops at the preview (no --promote / --publish) and re-reads to confirm"
      ]
    }
  },
  {
    "case_id": "themes.add-card-01",
    "type": "behavior",
    "skill": "shoplazza-themes",
    "operation": "themes.create",
    "scenario": "首页加一个客户评价模块 — discover addable cards before add_section, never invent a type",
    "input": "在主题 3f2a9c1e-7b4d-4e8a-9c21-5d6e7f809a1b 的首页加一个客户评价模块",
    "expected": {
      "route_to_skill": "shoplazza-themes",
      "access_tier": "leaf",
      "command_prefix": "themes section cards",
      "must_ask": false,
      "semantic_rubric": [
        "discovers candidates with themes section cards (theme_id, template index) before writing",
        "the planned add_section uses a returned items[].id as name, not an invented card type, via themes +edit in the task's session",
        "if several returned cards fit (e.g. testimonials vs customer reviews), lists them and asks instead of guessing",
        "does not jump to generating an AI card while an existing card fits"
      ],
      "params_must_contain": [
        "\"theme_id\":\"3f2a9c1e-7b4d-4e8a-9c21-5d6e7f809a1b\"",
        "\"template\":\"index\""
      ]
    }
  },
  {
    "case_id": "themes.global-config-01",
    "type": "behavior",
    "skill": "shoplazza-themes",
    "operation": "themes.update",
    "scenario": "全店按钮样式 → theme settings; the first step opens the session, then get-config locates the field by label",
    "input": "把主题 3f2a9c1e-7b4d-4e8a-9c21-5d6e7f809a1b 全店的按钮都改成圆角",
    "expected": {
      "route_to_skill": "shoplazza-themes",
      "access_tier": "shortcut",
      "command_prefix": "themes +page",
      "must_ask": false,
      "semantic_rubric": [
        "opens / reuses a session with themes +page on that theme, then reads themes session get-config (doc_id index, filtered to the button category) and locates the field by its label, not a guessed key",
        "the planned write is themes session update-config with only that one setting in --data settings, checked against the schema first; no theme_id in --params",
        "stops at the preview; if no such setting exists, says the theme doesn't support it instead of writing"
      ],
      "flags_must_contain": [
        "3f2a9c1e-7b4d-4e8a-9c21-5d6e7f809a1b"
      ]
    }
  }
]
