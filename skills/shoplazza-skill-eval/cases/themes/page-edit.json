[
  {
    "case_id": "themes.page-edit-01",
    "type": "behavior",
    "skill": "shoplazza-themes",
    "operation": "themes.update",
    "scenario": "改轮播图某一帧的标题 — first step opens the session on the named theme; the plan copies the block target and uses update_slot",
    "input": "主题 3f2a9c1e-7b4d-4e8a-9c21-5d6e7f809a1b 的首页，把轮播图第一张幻灯片的标题改成 Summer Sale",
    "expected": {
      "route_to_skill": "shoplazza-themes",
      "access_tier": "shortcut",
      "command_prefix": "themes +page",
      "flags_must_contain": [
        "3f2a9c1e-7b4d-4e8a-9c21-5d6e7f809a1b",
        "--template index"
      ],
      "flags_must_not_contain": [
        "--promote",
        "--publish",
        "--theme "
      ],
      "must_ask": false,
      "semantic_rubric": [
        "the first read is themes +page -t <that id> --template index (it returns the oseid)",
        "if later steps are shown (placeholders allowed): a single-card read with --session <oseid> --section <sid> --include schema, then themes +edit update_slot on the copied block-row target (<sid>.blocks[0] form) writing only the heading, Summer Sale verbatim",
        "no --promote / --publish anywhere; if later calls are shown they reuse the same --session and -t"
      ]
    }
  },
  {
    "case_id": "themes.add-card-01",
    "type": "behavior",
    "skill": "shoplazza-themes",
    "operation": "themes.create",
    "scenario": "首页加一个客户评价模块 — discover addable cards before add_section, never invent a type",
    "input": "在主题 3f2a9c1e-7b4d-4e8a-9c21-5d6e7f809a1b 的首页加一个客户评价模块",
    "expected": {
      "route_to_skill": "shoplazza-themes",
      "access_tier": "leaf",
      "command_prefix": "themes section cards",
      "must_ask": false,
      "semantic_rubric": [
        "discovers candidates with themes section cards (theme_id, template index) before any write",
        "if the add is shown, it is add_section via themes +edit in the task's session with a returned items[].id as name — never an invented card type",
        "does not jump to generating an AI card while an existing card might fit"
      ],
      "params_must_contain": [
        "\"theme_id\":\"3f2a9c1e-7b4d-4e8a-9c21-5d6e7f809a1b\"",
        "\"template\":\"index\""
      ]
    }
  },
  {
    "case_id": "themes.global-config-01",
    "type": "behavior",
    "skill": "shoplazza-themes",
    "operation": "themes.update",
    "scenario": "全店按钮样式 → theme settings; the first step opens the session, then get-config locates the field by label",
    "input": "把主题 3f2a9c1e-7b4d-4e8a-9c21-5d6e7f809a1b 全店的按钮都改成圆角",
    "expected": {
      "route_to_skill": "shoplazza-themes",
      "access_tier": "shortcut",
      "command_prefix": "themes +page",
      "must_ask": false,
      "semantic_rubric": [
        "the first step opens / reuses a session with themes +page on that theme",
        "if later steps are shown: themes session get-config (doc_id index, button category) to find the field by label, then themes session update-config writing only that setting; no theme_id in --params",
        "no --promote / --publish; does not guess a setting key before reading the schema"
      ],
      "flags_must_contain": [
        "3f2a9c1e-7b4d-4e8a-9c21-5d6e7f809a1b"
      ]
    }
  }
]
